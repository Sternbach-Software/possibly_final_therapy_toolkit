package com.example.navigationdrawertemplate.database
/*

import androidx.lifecycle.LiveData
import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query
import androidx.room.Update
import com.example.navigationdrawertemplate.ui.homePageConditions.Condition
import java.util.*

@Dao
interface ConditionDao {

    @Query("SELECT * FROM condition")
    fun getConditions(): LiveData<List<Condition>>

    @Query("SELECT * FROM condition WHERE id=(:id)")
    fun getCondition(id: UUID): LiveData<Condition?>

    @Update
    fun updateCondition(condition: Condition)

    @Insert
    fun addCondition(condition: Condition)
}*/
